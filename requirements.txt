Install:
1. sqlalchemy
2. fastapi
# 3. psycopg2-binary
4. uvicorn
5. pymsql

How to run app:
1. Open SQL shell and start the server (password is root, keep everything else default)
2. Open MySQLWorkbench and find the table
3. Use the command in the terminal: uvicorn main:app --reload
